// Renade RP. GGame Studio. 1.10.2017
// Server part of guntool package

/*
mp.events.add('guntool:loadRequest', function (player) {
    console.log('test ' + player.name);
    player.call('guntool:loadResponse');
});*/
